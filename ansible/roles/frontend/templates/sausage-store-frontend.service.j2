[Unit]
Description=frontend-sausage-store

[Service]
User={{ frontend_service_user }}
Restart=always
ExecStart=http-server {{ frontend_directory }} -p 4433 --proxy http://localhost:8080
[Install]
WantedBy=multi-user.target
